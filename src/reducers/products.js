let initialState = [
	{id:11456, name:'tv', cost: 1000},
	{id:2461, name:'mobile', cost: 3000}
]

export default function products(state=initialState, actions) {
	switch(actions.type) {
		default :
			return state
	}
}